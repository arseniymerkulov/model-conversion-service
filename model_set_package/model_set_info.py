class ModelSetInfo:
    def __init__(self, model_name, model_type, initial_format):
        self.model_name = model_name
        self.model_type = model_type
        self.initial_format = initial_format
